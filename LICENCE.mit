
from setuptools import setup, find_packages

setup(
    name="swiftrefactor",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "fastapi",
        "uvicorn",
        "astor",
        "pytest"
    ],
    entry_points={
        "console_scripts": [
            "swiftrefactor=swiftrefactor.cli:main"
        ]
    }
)